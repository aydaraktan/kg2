<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kg.geektech.last">

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_shama"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_shama_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Last">
        <activity
            android:name=".rpesentation.DetailActivity"
            android:exported="false" />
        <activity
            android:name=".rpesentation.task.TaskActivity2"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".rpesentation.main.MainActivity"
            android:exported="true"/>
    </application>

</manifest>